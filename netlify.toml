[build]
  base    = "bieli-frontend"
  command = "yarn install && yarn build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"

[[redirects]]
  from   = "/api/*"
  to     = "https://mes-projet-production.up.railway.app/api/:splat"
  status = 200
  force  = true
